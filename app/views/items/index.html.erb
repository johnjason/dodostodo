<h1>Meine Aufgaben</h1>
<%= link_to "Neue Aufgabe", new_item_path, :class=>"btn btn-default", :type=>"btn" %> 
<hr>

<% @items.each do |item| %>
    <h2><%= link_to item.title, item %></h2>
    <p><%= item.description %></p>
<% end %>